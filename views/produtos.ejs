<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Produtos Inúteis - Produtos</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Quicksand:wght@400;500;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
  <link rel="stylesheet" href="/css/styles.css">
  <link rel="stylesheet" href="/css/styles-extras.css">
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container">
      <a class="navbar-brand" href="/">Produtos Inúteis <span class="useless-tag">100% inútil</span></a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item">
            <a class="nav-link" href="/"><i class="fas fa-home me-1"></i> Home</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/clientes"><i class="fas fa-users me-1"></i> Clientes</a>
          </li>
          <li class="nav-item">
            <a class="nav-link active" href="/produtos"><i class="fas fa-box-open me-1"></i> Produtos</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/pedidos"><i class="fas fa-shopping-cart me-1"></i> Pedidos</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>

  <div class="container mt-4">
    <h1 class="mb-4">Catálogo de Produtos Inúteis</h1>
    
    <div class="product-filter">
      <button class="filter-btn active" data-filter="all">Todos</button>
      <button class="filter-btn" data-filter="Utilidades">Utilidades</button>
      <button class="filter-btn" data-filter="Tecnologia">Tecnologia</button>
      <button class="filter-btn" data-filter="Pet">Pet</button>
    </div>
    
    <div class="row products-grid">
      <% produtos.forEach(produto => { %>
        <div class="col-md-4 mb-4 product-item" data-category="<%= produto.categoria %>">
          <div class="product-card card h-100">
            <div class="product-image-container">
              <% if (produto.id === 1) { %>
                <img src="/img/canetasemtinta.png" class="product-image" alt="<%= produto.nome %>">
              <% } else if (produto.id === 2) { %>
                <img src="/img/guardachuvafurado.png" class="product-image" alt="<%= produto.nome %>">
              <% } else if (produto.id === 3) { %>
                <img src="/img/carrosel1.png" class="product-image" alt="<%= produto.nome %>">
              <% } else if (produto.id === 4) { %>
                <img src="/img/oculosspray.png" class="product-image" alt="<%= produto.nome %>">
              <% } else if (produto.id === 5) { %>
                <img src="/img/tecladosemletras.png" class="product-image" alt="<%= produto.nome %>">
              <% } else if (produto.id === 6) { %>
                <img src="/img/espelhoparavampiro.png" class="product-image" alt="<%= produto.nome %>">
              <% } else if (produto.id === 7) { %>
                <img src="/img/oculosspray.png" class="product-image" alt="<%= produto.nome %>">
              <% } else if (produto.id === 8) { %>
                <img src="/img/guardachuvafurado.png" class="product-image" alt="<%= produto.nome %>">
              <% } else { %>
                <img src="/img/wifisubmarino.png" class="product-image" alt="<%= produto.nome %>">
              <% } %>
              <div class="product-badge"><%= produto.categoria %></div>
            </div>
            <div class="card-body">
              <h5 class="card-title"><%= produto.nome %></h5>
              <p class="card-text description"><%= produto.descricao %></p>
              <div class="product-details">
                <div class="product-price">R$ <%= produto.preco.toFixed(2) %></div>
                <div class="product-stock">
                  <span class="stock-badge <%= produto.estoque < 20 ? 'low-stock' : '' %>">
                    <i class="fas fa-cubes me-1"></i> <%= produto.estoque %>
                  </span>
                </div>
              </div>
              <a href="/produtos/<%= produto.id %>" class="btn btn-primary mt-3 w-100">
                <i class="fas fa-eye me-2"></i> Ver Produto
              </a>
            </div>
          </div>
        </div>
      <% }); %>
    </div>
    
    <div class="fun-facts mt-5">
      <h3 class="section-title mb-4">Fatos Inúteis Sobre Nossos Produtos</h3>
      <div class="fun-fact-item">
        <div class="fun-fact-icon"><i class="fas fa-lightbulb"></i></div>
        <div class="fun-fact-content">
          <p class="mb-0">Nosso Guarda-Chuva Furado economiza até 97% da água da chuva, permitindo que ela volte rapidamente ao ciclo natural da água.</p>
        </div>
      </div>
      <div class="fun-fact-item">
        <div class="fun-fact-icon"><i class="fas fa-exclamation-triangle"></i></div>
        <div class="fun-fact-content">
          <p class="mb-0">O Teclado sem Letras já ajudou mais de 1.000 pessoas a decorar a posição das teclas em tempo recorde!</p>
        </div>
      </div>
      <div class="fun-fact-item">
        <div class="fun-fact-icon"><i class="fas fa-magic"></i></div>
        <div class="fun-fact-content">
          <p class="mb-0">Nosso WiFi para Submarino é feito com tecnologia 100% imaginária, garantindo conexão invisível em qualquer profundidade!</p>
        </div>
      </div>
    </div>
  </div>

  <footer class="bg-dark text-white py-3">
    <div class="container">
      <div class="footer-content">
        <div>
          <div class="social-icons">
            <a href="#"><i class="fab fa-facebook-f"></i></a>
            <a href="#"><i class="fab fa-twitter"></i></a>
            <a href="#"><i class="fab fa-instagram"></i></a>
            <a href="#"><i class="fab fa-tiktok"></i></a>
          </div>
          <div class="footer-links">
            <a href="#">Sobre Nós</a>
            <a href="#">FAQ</a>
            <a href="#">Política de Devoluções</a>
            <a href="#">Contato</a>
          </div>
        </div>
        <div>
          <p class="mb-0 d-flex align-items-center">
            <i class="fas fa-headset me-2"></i> Suporte: (11) 4002-8922
          </p>
          <p class="mb-0 d-flex align-items-center">
            <i class="fas fa-envelope me-2"></i> contato@produtosinuteis.com
          </p>
        </div>
      </div>
      <div class="text-center copyright">
        <p> 2023 Produtos Inúteis - Desperdiçando seu dinheiro desde sempre</p>
      </div>
    </div>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <script src="/js/products.js"></script>
</body>
</html>